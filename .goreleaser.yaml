# GoReleaser configuration for Ramorie CLI
version: 2

project_name: ramorie

before:
  hooks:
    - go mod tidy

builds:
  - id: ramorie
    main: ./cmd/jbraincli
    binary: ramorie
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: ramorie
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - name: ramorie
    repository:
      owner: terzigolu
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://ramorie.com"
    description: "AI-powered task and memory management CLI"
    license: "MIT"
    install: |
      bin.install "ramorie"
    test: |
      system "#{bin}/ramorie", "--version"

release:
  github:
    owner: terzigolu
    name: ramorie
  draft: false
  prerelease: auto
